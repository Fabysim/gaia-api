Page de Récupération

-Demande de l'adresse mail
Sécurisation et vérification de la validité de l'adresse mail
correspondance avec une autre adresse mail(mt_rand()*8)
BDD=> Table "recuperation" (id, mail, code)
stockage du mail dans $_SESSION['recupmail']
stackage du code dans $_SESSION['recupcode']
redirection vers la page d'entrée du code ($_GET['section']='code')
si ok => redirection vers la page de changement de mot de passe (+ confirmation du mdp)
($_GET['section']='mdpform')
si les deux mots de passe correspondent => hashage en sha1( et enrégitrement dans la BDD)








partie frontend

il faut une page de récupération de mot de passe.

qui demande de saisir l'adresse email..

qui contient un formulaire qui à un placeholder="votre adresse email" et un   "name='email' " 
